# 可见性、剖切与测量

## 可见性工具栏

- **隐藏选中**：隐藏当前选中构件。
- **隔离**：只显示选中构件。
- **X-Ray**：半透明显示全部构件。
- **重置可见性**：恢复全部构件可见。

## 剖切

通过 X / Y / Z 三个方向的滑杆移动剖切面，用于观察模型内部结构。

## 测量

距离测量：依次点击两点，显示两点间距；双击结束测量，可清除。

## 其他视图工具

- **NavCube**：方向指示与视图切换。
- **复位视角**：恢复默认相机位置。
- 轨道旋转 / 缩放 / 平移为 xeokit 默认交互。
