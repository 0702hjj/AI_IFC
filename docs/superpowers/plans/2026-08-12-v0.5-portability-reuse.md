# v0.5 可移植复用 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把「可复用性优先」推进到对外可消费：对接契约文档、skill 版本化 + Release 分发、主 Agent 编排提示词包（统合 W-0017 与 plan→cad→ifc 推荐路径）。

**Architecture:** spec 已定稿（`docs/superpowers/specs/2026-08-12-portability-reuse-design.md`）。代码改动集中在 `tools/skill_pack.py`（版本化打包）；其余为文档（docs/site 公开站）与新 skill 目录（`skills/aibim-orchestrator/`）。不改 web/server/services 业务代码。

**Tech Stack:** Python 3（stdlib unittest，`tests/skill/`）、VitePress（docs/site）、Markdown skill 包（SKILL.md frontmatter 契约）。

## Global Constraints

- 迭代分支 `feat/v0.5-portability-reuse`（已建，spec 已在其上 commit 6945c81）；一天最多 1 PR，工作项在本分支累积，收工一次提。
- commit 信息中文、前缀式（`feat(tools): ...` / `docs: ...` / `test(skill): ...`）。
- 测试纪律：代码变更先失败测试后实现；skill 测试命令 `python -m pytest tests/skill/ -q`。
- MIT 边界：`skills/aidxfv/v1`、`skills/aidxfv/v2`、`skills/aiblueprint-mcp` 保留 MIT 归属（各自 LICENSE / frontmatter upstream），勿与主仓 AGPL 文件混排；新 skill `aibim-orchestrator` 为主仓 AGPL-3.0-only。
- `docs/site/public/` 生成物只经 `cd docs && npm run gen:api` 更新；本计划不改 API，不需要跑。
- 内部文档（`docs/work/`、`docs/superpowers/`）内容不得复制进 `docs/site/`。
- 文档站校验：`cd docs && npm run docs:build` 必须绿（含死链检查）。
- AGENTS.md 的计数（如 skill 测试数）在有变化的任务收尾时同步。

---

### Task 1: 工作项立项（W-0026..W-0030）+ PLAN v0.5 milestone 行

**Files:**
- Create: `docs/work/items/W-0026-integration-contract-docs.md`
- Create: `docs/work/items/W-0027-services-ifc-docker-smoke.md`
- Create: `docs/work/items/W-0028-skill-versioned-release.md`
- Create: `docs/work/items/W-0029-orchestrator-prompt-pack.md`
- Create: `docs/work/items/W-0030-services-cad-direction.md`
- Modify: `docs/work/PLAN-v0.1.0.md`（milestone 表加 v0.5 行）

**Interfaces:**
- Produces: W-0026..W-0030 五个 item ID，后续 Task 2-6 各自认领并回写状态。

- [ ] **Step 1: 写五个 item 文件**

模板遵循 `docs/work/README.md`（状态/优先级/Milestone/来源/执行者/背景/涉及位置/方案/验收标准/测试要求）。关键内容：

- W-0026「对接契约文档（存储 + 前端）」：状态 open，P1，Milestone v0.5，来源 spec 2026-08-12-portability-reuse-design.md §2。验收：两页文档入站、nav 更新、`npm run docs:build` 绿。测试要求：文档任务无单测，docs:build 为门禁。
- W-0027「services/ifc Dockerfile 冒烟 + 独立部署文档对账」：状态 open，P1。验收：镜像构建成功、容器起后 `GET /openapi.json` 200、`docs/site/guide/services-ifc.md`（+en）补 Docker 段。测试要求：冒烟命令序列记入 item。
- W-0028「skill 版本化 + Release 分发」：状态 open，P1。验收：5 个 SKILL.md 带 `version`、各有 CHANGELOG、archive 命名 `<name>-<version>.tar.gz`、缺 version 打包报错、安装/发布文档入站。测试要求：packer 变更 TDD，tests/skill 全绿。
- W-0029「主 Agent 编排提示词包（aibim-orchestrator）」：状态 open，P1。验收：`skills/aibim-orchestrator/` 可被打包（registry + 契约测试）、接力契约 fixture 齐、W-0017 关闭、两份 spec 修订。测试要求：tests/skill 新增真实 skill 校验/打包测试。
- W-0030「services/cad 同构方向立项」：状态 open，P2，本轮仅立项不实施。方案段写：与 services/ifc 完全同构——DXF 生成脚本为唯一事实源、版本快照、实体级语义 diff、REST 编辑 API 同形（PUT /script → run → save → locate）；下迭代先补独立 spec。验收：item 存在且方向写清。

- [ ] **Step 2: PLAN 加 v0.5 行**

`docs/work/PLAN-v0.1.0.md` milestone 表末尾加：

```markdown
| v0.5 | 可移植复用（spec: 2026-08-12-portability-reuse-design.md，分支 feat/v0.5-portability-reuse）| W-0026..W-0029（W-0030 仅立项） | 对接契约入站；services/ifc 镜像冒烟通过；skill 版本化 + Release 流程走通；aibim-orchestrator 提示词包可打包、W-0017 关闭 |
```

- [ ] **Step 3: Commit**

```bash
git add docs/work/items/W-002*.md docs/work/items/W-0030*.md docs/work/PLAN-v0.1.0.md
git commit -m "docs(work): W-0026..W-0030 立项 + PLAN v0.5 milestone 行"
```

---

### Task 2: skill_pack 版本化（TDD）+ 5 个 skill 补 version/CHANGELOG

认领 W-0028（状态改 in-progress，执行者/分支填 feat/v0.5-portability-reuse）。

**Files:**
- Modify: `tools/skill_pack.py`（`_validate_frontmatter`、`build`、新增 `skill_version`）
- Test: `tests/skill/test_skill_pack.py`
- Modify: `skills/aiifc/SKILL.md`、`skills/aidxfv/SKILL.md`、`skills/aidxfv/v1/SKILL.md`、`skills/aidxfv/v2/SKILL.md`、`skills/aiblueprint-mcp/SKILL.md`（frontmatter 加 `version: 0.1.0`）
- Create: `skills/aiifc/CHANGELOG.md`、`skills/aidxfv/CHANGELOG.md`、`skills/aidxfv/v1/CHANGELOG.md`、`skills/aidxfv/v2/CHANGELOG.md`、`skills/aiblueprint-mcp/CHANGELOG.md`
- Create: `skills/aiblueprint-mcp/requirements.txt`（base 契约缺它，内容为一行注释 `# no python dependencies; MCP server driven via opencode.json`）

**Interfaces:**
- Produces: `packer.skill_version(skill_root: Path) -> str`（读 frontmatter `version`，缺省返回 `""`）；archive 产物命名 `{skill_name}-{version}.tar.gz`；注册 skill（registry 内有条目者）frontmatter 必须带非空 `version`。

- [ ] **Step 1: 写失败测试**

`tests/skill/test_skill_pack.py` 修改：

1. fixture `SKILL_MD` 加 version 行：

```python
SKILL_MD = """---
name: {name}
description: test skill
version: 0.1.0
---
# Test
"""
```

2. `TestSkillPackValidate` 新增：

```python
    def test_validate_registered_skill_missing_version(self):
        with tempfile.TemporaryDirectory() as tmp:
            skill = _make_skill(Path(tmp), "aiifc")
            (skill / "SKILL.md").write_text(
                SKILL_MD.format(name="aiifc").replace("version: 0.1.0\n", ""),
                encoding="utf-8",
            )
            with self.assertRaises(ValueError):
                packer.validate(skill)

    def test_validate_unknown_skill_version_not_enforced(self):
        with tempfile.TemporaryDirectory() as tmp:
            skill = _make_skill(Path(tmp), "unregistered")
            (skill / "SKILL.md").write_text(
                SKILL_MD.format(name="whatever").replace("version: 0.1.0\n", ""),
                encoding="utf-8",
            )
            packer.validate(skill, strict_noise=False)

    def test_real_skills_have_version(self):
        for root, name in (
            (REAL_SKILL, "aiifc"),
            (REAL_CAD_SKILL, "aidxfv1"),
            (REAL_CAD_SKILL_V2, "aidxfv2"),
        ):
            self.assertTrue(
                packer.skill_version(root),
                f"{name} SKILL.md frontmatter 缺 version",
            )
```

3. `TestSkillPackBuild` 新增：

```python
    def test_build_archive_requires_version(self):
        with tempfile.TemporaryDirectory() as tmp:
            root = Path(tmp)
            skill = _make_skill(root, "unregistered")
            (skill / "SKILL.md").write_text(
                SKILL_MD.format(name="unregistered").replace("version: 0.1.0\n", ""),
                encoding="utf-8",
            )
            with self.assertRaises(ValueError):
                packer.build(
                    skill_root=skill, output_root=root / "out",
                    skill_name="unregistered", archive=True,
                )

    def test_build_archive_name_includes_version(self):
        with tempfile.TemporaryDirectory() as tmp:
            root = Path(tmp)
            skill = _make_skill(root, "aiifc")
            out = root / "out"
            result = packer.build(
                skill_root=skill, output_root=out, skill_name="aiifc", archive=True
            )
            self.assertEqual(result.archive_path, out / "aiifc-0.1.0.tar.gz")
```

4. 既有 `test_real_cad_skill_cli_archive_contains_license` 的断言改为版本化命名：

```python
            version = packer.skill_version(REAL_CAD_SKILL)
            archive = Path(tmp) / "dist" / f"aidxfv1-{version}.tar.gz"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/skill/test_skill_pack.py -q`
Expected: FAIL——`skill_version` 不存在（AttributeError）、archive 名不带版本、真实 skill 缺 version。

- [ ] **Step 3: 实现 packer 变更**

`tools/skill_pack.py`：

1. `_validate_frontmatter` 末尾（name 匹配检查之后）加：

```python
    if skill_name is not None and not fields.get("version", ""):
        raise ValueError(
            f"SKILL.md frontmatter version is empty (skill {skill_name!r})"
        )
```

2. 新增公开 helper（放 `_parse_frontmatter` 之后）：

```python
def skill_version(skill_root: Path) -> str:
    """Return the SKILL.md frontmatter version, or "" when absent."""
    return _parse_frontmatter(skill_root).get("version", "")
```

3. `build()` 的 archive 段改为：

```python
    archive_path = None
    if archive:
        version = skill_version(skill_root)
        if not version:
            raise ValueError(
                "SKILL.md frontmatter version is empty; "
                "cannot produce a versioned archive name"
            )
        archive_path = output_root / f"{skill_name}-{version}.tar.gz"
        log(f"Creating archive: {archive_path}")
        with tarfile.open(archive_path, "w:gz") as tar:
            tar.add(dest, arcname=skill_name)
```

注意：`build()` 里 archive 命名在 copy/validate 之后，缺 version 的报错时机在拷贝完成后——可接受（dest 已清理重建，无副作用泄漏）；不要为报错提前到 validate 而改 validate 签名。

- [ ] **Step 4: 5 个 SKILL.md 加 version + 建 CHANGELOG**

每个 frontmatter 在 `description:` 行后加 `version: 0.1.0`（保持既有 key 风格）。各 skill 建 `CHANGELOG.md`：

```markdown
# Changelog

## 0.1.0 - 2026-08-12

- 首个版本化发布：frontmatter 引入 `version`，打包产物命名 `<name>-<version>.tar.gz`，经 GitHub Release 分发。
```

（aidxfv/v1、aidxfv/v2 的 CHANGELOG 各加一行 MIT 归属提示：`MIT; upstream earthtojake/text-to-cad，见 LICENSE`。aiblueprint-mcp 加 `MIT; upstream thebossnow/aiblueprint-mcp`。）

建 `skills/aiblueprint-mcp/requirements.txt`：

```
# no python dependencies; the MCP server is driven via opencode.json
```

- [ ] **Step 5: 跑全部 skill 测试确认绿**

Run: `python -m pytest tests/skill/ -q`
Expected: 全 PASS（含真实 skill 打包归档测试，版本化命名断言通过）。

- [ ] **Step 6: Commit**

```bash
git add tools/skill_pack.py tests/skill/test_skill_pack.py skills/*/SKILL.md skills/aidxfv/v*/SKILL.md skills/*/CHANGELOG.md skills/aidxfv/v*/CHANGELOG.md skills/aiblueprint-mcp/requirements.txt
git commit -m "feat(tools): skill 版本化——frontmatter version 强制（注册 skill）+ 归档命名 <name>-<version>.tar.gz + 各 skill CHANGELOG"
```

---

### Task 3: skill 安装/发布文档（W-0028 收尾）

**Files:**
- Create: `docs/site/guide/skills.md`（《skill 获取与安装》）
- Create: `docs/site/en/guide/skills.md`
- Create: `docs/site/development/release.md`（发布流程 runbook，仅中文——en 站是子集，Development 组 en 只有 architecture）
- Modify: `docs/site/.vitepress/config.mts`（zh guide sidebar 加 skills；zh development sidebar 加 release；en guide sidebar 加 skills）
- Modify: `docs/work/items/W-0028-skill-versioned-release.md`（状态 done，关闭于填本迭代 PR 占位「本迭代 PR」，合并后 PR 号在 Task 6 统一回填——不，直接在合并前提 PR 时回填见 Task 6；本任务先把状态留 in-progress，Task 6 统一收口。改为：本任务内不碰 item 状态）

**Interfaces:**
- Consumes: Task 2 的归档命名 `{name}-{version}.tar.gz`、5 个 skill 的 version 0.1.0。

- [ ] **Step 1: 写 `docs/site/guide/skills.md`**

结构（内容按此写实，不留 TODO）：

```markdown
# AI Skill 获取与安装

## 有哪些 skill
（表格：aiifc=IFC 生成/修改（LGPL-3.0 文档包）；aidxfv1/aidxfv2=CAD/DXF 生成（MIT）；aiblueprint-mcp=DXF 交互微调 MCP（MIT）；aibim-orchestrator=主 Agent 编排提示词包（AGPL，Task 5 交付，本页先列位置与一句简介，标注"随 v0.5 提供"——若 Task 5 未先完成则此处只列四个，Task 5 负责补行）

## 下载
GitHub Release 页面下载 `<name>-<version>.tar.gz`；或仓库内自助打包：
python tools/skill_pack.py --skill aiifc --archive
python tools/skill_pack.py --skill aidxfv1 --skill-dir skills/aidxfv/v1 --archive
产物在 skills/dist/。

## 安装
解压到 agent 运行时的 skill 目录：
- opencode: ~/.agents/skills/<name>/ 或项目 .opencode/skill 等价目录（以运行时文档为准）
- Claude Code: ~/.claude/skills/<name>/
安装后 SKILL.md 的 name/description 会被运行时索引，无需额外注册。

## 运行依赖
aiifc 需要 Python 环境装 ifcopenshell/ifcquery/numpy（见包内 requirements.txt）；aidxfv 需要 ezdxf；aiblueprint-mcp 是 MCP server 形态，按包内 README/opencode.json 接入。
```

en 版写对应英文页。

- [ ] **Step 2: 写 `docs/site/development/release.md`**

结构：

```markdown
# 发布流程

## skill 发布（手工 runbook）
1.  bump：改对应 skill SKILL.md 的 version + CHANGELOG 条目（语义化版本）
2.  打包：python tools/skill_pack.py --skill <name> --archive（aidxfv1/2 需 --skill-dir）
3.  校验：python -m pytest tests/skill/ -q 全绿
4.  发布：git tag skill-<name>-<version> && gh release create skill-<name>-<version> skills/dist/<name>-<version>.tar.gz
（CI 自动化本轮不做，见 spec 2026-08-12 §3）

## 平台发布
（指向 project/changelog.md 与既有 v0.1.0 发布实践，一段即可）
```

- [ ] **Step 3: nav 更新**

`config.mts`：zh「快速开始」items 末尾加 `{ text: 'AI Skill 获取与安装', link: '/guide/skills' }`；zh「开发指南」items 末尾加 `{ text: '发布流程', link: '/development/release' }`；en「Quick Start」items 末尾加 `{ text: 'AI Skills: Get & Install', link: '/en/guide/skills' }`。

- [ ] **Step 4: 构建验证**

Run: `cd docs && npm run docs:build`
Expected: 构建成功无死链报错。

- [ ] **Step 5: Commit**

```bash
git add docs/site/guide/skills.md docs/site/en/guide/skills.md docs/site/development/release.md docs/site/.vitepress/config.mts
git commit -m "docs(site): skill 获取与安装 + 发布流程 runbook（W-0028 文档段）"
```

---

### Task 4: 对接契约文档（W-0026）

**Files:**
- Create: `docs/site/development/storage-integration.md`（存储接口契约）
- Create: `docs/site/development/frontend-integration.md`（前端对接契约）
- Modify: `docs/site/.vitepress/config.mts`（zh development sidebar 加两页；en 不加，en Development 是子集）

**Interfaces:**
- Consumes: 无代码依赖；事实来源 `server/internal/store/store.go`、`server/cmd/server/main.go`（env 开关）、`docs/site/reference/edit-api.md`、`docs/site/public/go-rest-api.routes.json`（生成物，只引用不改）。

- [ ] **Step 1: 写存储接口契约页**

先读 `server/internal/store/store.go` 拿到 Store 接口的真实方法签名与 `server/cmd/server/main.go` 的装配逻辑（`VIEWER_PG_DSN` 非空走 PG，否则文件存储），以代码为准。页面结构：

```markdown
# 存储接口契约（整合进已有数据库）

## 两种存储实现
- 文件存储（默认，零依赖）：VIEWER_DATA_DIR 指向的数据目录
- PostgreSQL（可选）：设 VIEWER_PG_DSN 即切换，schema 见 server/internal/{store,override,change,issue}/pgstore.go

## 数据目录布局
（以代码与 AGENTS.md 为准列目录树：uploads/ scripts/ versions/ *.map.json 等；强调 server 与 services/ifc 必须共享同一 VIEWER_DATA_DIR 绝对路径）

## 第三方整合路径
1. 实现 Go store 接口（方法面列自 store.go，逐方法一句职责）→ 用自己的数据库
2. 复用本仓 PG schema → 与本仓 PG 实例共存/ federation
3. 仅文件级对接：读写 VIEWER_DATA_DIR 目录（最低耦合，注意并发写纪律）

## 边界
services/ifc 直接读写文件目录（不经过 Go store）；PG 只存 override/change/issue 等网关侧状态（以 pgstore.go 实际为准）。
```

- [ ] **Step 2: 写前端对接契约页**

先读 `docs/site/reference/edit-api.md`、`docs/site/reference/rest-api.md` 与 `server/internal/api/api.go` 的 envelope/鉴权段，以代码为准。页面结构：

```markdown
# 前端对接契约（自研/第三方前端接入）

## web/ 是参考实现
平台本体是 API；web/ 可整体替换。对接面 = Go 网关 :8090 的 /api/v1/*（OpenAPI：reference/openapi.md 与生成物 go-rest-api.routes.json）。

## 协议基线
- envelope {code,message,data}，code=0 成功；错误码段（40100 鉴权、50400 diff 超时等，列自 api.go）
- 鉴权：VIEWER_API_TOKEN 设置后 Bearer token；CORS 白名单 VIEWER_CORS_ORIGINS
- SSE：chat/事件流的连接与事件格式（以 server 代码为准列端点）

## 编辑流程对接（script-as-source）
PUT /script（暂存）→ script/run（沙箱试跑）→ script/save（大版本）→ GET /script/locate?guid= 定位；版本与 diff 端点。逐步给 curl 示例（照 edit-api.md 既有示例风格）。

## 显示对接
XKT 产物经 GET /v1/models/{id}/... 只读文件端点获取（鉴权豁免以 api.go 为准）；自研前端可用任意 XKT/IFC 渲染器。
```

- [ ] **Step 3: nav + 构建验证**

zh「开发指南」items 加 `{ text: '存储接口契约', link: '/development/storage-integration' }` 与 `{ text: '前端对接契约', link: '/development/frontend-integration' }`。
Run: `cd docs && npm run docs:build` — Expected: 绿。

- [ ] **Step 4: 关 item + Commit**

W-0026 状态改 done（关闭于填「本迭代 PR（feat/v0.5-portability-reuse）」，Task 6 统一校订为实际 PR 号）。

```bash
git add docs/site/development/storage-integration.md docs/site/development/frontend-integration.md docs/site/.vitepress/config.mts docs/work/items/W-0026-integration-contract-docs.md
git commit -m "docs(site): 存储/前端对接契约页（W-0026）"
```

---

### Task 5: services/ifc Dockerfile 冒烟 + 独立部署文档对账（W-0027）

**Files:**
- Modify: `docs/site/guide/services-ifc.md`（补 Docker 段）
- Modify: `docs/site/en/guide/services-ifc.md`
- Modify: `services/ifc/Dockerfile`（仅当冒烟发现实际问题时才动；预期不动）
- Modify: `docs/work/items/W-0027-services-ifc-docker-smoke.md`

**Interfaces:**
- Consumes: 既有 `services/ifc/Dockerfile`（构建上下文=仓库根，因 COPY skills/aiifc/references/docs/flows）。

- [ ] **Step 1: 构建镜像**

```bash
docker build -f services/ifc/Dockerfile -t aiifc-edit-service:v0.5 .
```
Expected: 构建成功。若失败，按报错修 Dockerfile 并把修复记入 commit（先在本文件记录失败现象再改——系统性调试）。

- [ ] **Step 2: 跑容器冒烟**

```bash
mkdir -p /tmp/opencode/editvc-data
docker run --rm -d --name editvc-smoke -p 18100:8100 -v /tmp/opencode/editvc-data:/data aiifc-edit-service:v0.5
curl -sf http://127.0.0.1:18100/openapi.json | head -c 200
docker stop editvc-smoke
```
Expected: `/openapi.json` 返回 200 JSON（FastAPI 默认路由，证明应用起来了）。把命令序列与结果写进 W-0027 item 的验收记录。

- [ ] **Step 3: 文档补 Docker 段**

`docs/site/guide/services-ifc.md` 在现有独立部署内容后加「Docker 单容器部署」小节：构建命令（强调上下文是仓库根）、运行命令（挂数据卷、端口映射）、与 Go 网关的关系（本容器只是业务核心，对外完整平台仍需 server，或 AI agent 可直连 :8100，注意无鉴权保持 127.0.0.1/内网）。en 版同步。

- [ ] **Step 4: 构建验证 + 关 item + Commit**

Run: `cd docs && npm run docs:build` — Expected: 绿。W-0027 状态 done。

```bash
git add docs/site/guide/services-ifc.md docs/site/en/guide/services-ifc.md docs/work/items/W-0027-services-ifc-docker-smoke.md
git commit -m "docs(site): services/ifc Docker 单容器部署段 + 冒烟验证（W-0027）"
```

---

### Task 6: aibim-orchestrator 提示词包 skill（W-0029 主体）

**Files:**
- Create: `skills/aibim-orchestrator/SKILL.md`
- Create: `skills/aibim-orchestrator/requirements.txt`（一行注释 `# prompt pack; no python dependencies`）
- Create: `skills/aibim-orchestrator/CHANGELOG.md`（0.1.0，AGPL-3.0-only）
- Create: `skills/aibim-orchestrator/references/SUBAGENTS.md`
- Create: `skills/aibim-orchestrator/references/RELAY_CONTRACT.md`
- Create: `skills/aibim-orchestrator/references/fixtures/plan.sample.json`
- Create: `skills/aibim-orchestrator/examples/opencode/agent/aibim-orchestrator.md`
- Create: `skills/aibim-orchestrator/examples/opencode/agent/ifc-agent.md`
- Create: `skills/aibim-orchestrator/examples/opencode/agent/cad-agent.md`
- Modify: `tools/skill_pack.py`（SKILL_REGISTRY 加条目）
- Test: `tests/skill/test_skill_pack.py`

**Interfaces:**
- Produces: registry 条目 `"aibim-orchestrator"`；真实 skill 可通过 `packer.validate` / `build(archive=True)`（名 `aibim-orchestrator-0.1.0.tar.gz`）。

- [ ] **Step 1: 写失败测试**

`tests/skill/test_skill_pack.py` 加常量与测试：

```python
REAL_ORCH_SKILL = REPO_ROOT / "skills" / "aibim-orchestrator"
```

```python
    def test_validate_real_orchestrator_skill_passes(self):
        packer.validate(REAL_ORCH_SKILL, strict_noise=False)

    def test_real_orchestrator_skill_builds_and_archives(self):
        with tempfile.TemporaryDirectory() as tmp:
            out = Path(tmp) / "dist"
            result = packer.build(
                skill_root=REAL_ORCH_SKILL,
                output_root=out,
                skill_name="aibim-orchestrator",
                archive=True,
            )
            self.assertIsNotNone(result.archive_path)
            with tarfile.open(result.archive_path, "r:gz") as tar:
                names = tar.getnames()
            self.assertIn("aibim-orchestrator/SKILL.md", names)
            self.assertIn("aibim-orchestrator/references/RELAY_CONTRACT.md", names)
            self.assertIn("aibim-orchestrator/references/SUBAGENTS.md", names)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/skill/test_skill_pack.py -q -k orchestrator`
Expected: FAIL（FileNotFoundError: 目录不存在 / registry 无条目）。

- [ ] **Step 3: registry 加条目**

`tools/skill_pack.py` SKILL_REGISTRY 加：

```python
    "aibim-orchestrator": (
        "references/SUBAGENTS.md",
        "references/RELAY_CONTRACT.md",
        "references/fixtures/plan.sample.json",
        "examples/opencode/agent/aibim-orchestrator.md",
        "CHANGELOG.md",
    ),
```

- [ ] **Step 4: 写 SKILL.md（主 Agent system prompt 本体）**

frontmatter：

```markdown
---
name: aibim-orchestrator
description: 主 Agent 编排提示词包——plan→cad→ifc 推荐路径的意图路由与子 Agent 派发契约。Use when a main/designer-facing agent needs to route tasks to IFC (aiifc) or CAD (aidxfv) subagents, or chain the full plan.json → DXF → IFC pipeline. Agent-agnostic; ships opencode agent config examples.
version: 0.1.0
license: AGPL-3.0-only
---
```

正文骨架（按此写实）：

```markdown
# AIBIM 主 Agent 编排

## 角色
你是与设计师对话的唯一入口。你不直接建模/画图：判断意图 → 派生子 Agent → 汇总结果回报设计师。

## 意图路由表
| 设计师输入 | 派发 |
| IFC 生成/修改（墙、板、层高、构件参数） | ifc-agent（skills/aiifc） |
| DXF 生成/修改（平面、户型、门窗墙体 2D） | cad-agent（skills/aidxfv） |
| DXF 逐实体核查/量测/微调 | cad-agent + aiblueprint-mcp（见 skills/aiblueprint-mcp 分工原则） |
| 从想法到交付的全链路 | plan→cad→ifc 接力（references/RELAY_CONTRACT.md） |
| 设计规范/审查问答 | 直接回答，不派发 |

## 派发纪律
1. 子 Agent 提示词模板见 references/SUBAGENTS.md（输入契约/输出契约/边界各就各位）。
2. 一次一派发，等子 Agent 报告再决定下一步；不并行派两个写同一产物的子 Agent。
3. 子 Agent 报告即事件载荷：含产物路径、版本号、validate 结果；汇总时原样转述关键字段，不编造。
4. 接力链路每个锚点必须给设计师确认（plan.json 确认 → DXF 确认 → IFC 交付）。

## 边界
- 事件总线/代码级 orchestrator 不是本包依赖；本包是纯提示词 + 数据契约。
- 事实源纪律：IFC 段脚本为唯一事实源（aiifc SKILL.md MUST #25-31）；CAD 段以 aidxfv 的 gen_dxf() 源为准。
```

- [ ] **Step 5: 写 references/SUBAGENTS.md（子 Agent 分工契约）**

结构（写实）：

```markdown
# 子 Agent 分工契约

## ifc-agent
- 技能来源：skills/aiifc（IfcOpenShell，script-as-source）
- 输入：设计师确认后的需求/plan.json 锚点 + 既有脚本（修改场景）
- 输出：构建脚本路径 + 运行产物 IFC 路径 + validate/design_review 结果
- 边界：只写脚本与派生物；不改 DXF；不直接与设计师对话
- 报告格式：{产物路径, 版本, validate 结果, 遗留问题}

## cad-agent
- 技能来源：skills/aidxfv（v1 通用 / v2 建筑平面管线）
- 输入：plan.json（接力场景）或自然语言平面需求
- 输出：DXF 路径 + 校验结果 +（v2 管线）building.json
- 边界：只写 DXF/building.json；IFC 转换不归它
- 报告格式：同上

## 主 Agent 派发提示词模板
（各给一段可直接填进 task/子会话的模板，含输入占位与报告格式要求）
```

- [ ] **Step 6: 写 references/RELAY_CONTRACT.md + fixture**

内容要点（写实，锚点与 aidxfv v2 的 plan.json 约定对齐——先读 `skills/aidxfv/v2/` 的 steps/references 拿真实字段名再写，不臆造）：

```markdown
# 接力数据契约：plan.json → DXF → IFC

## 锚点 1：plan.json
（字段表：以 aidxfv v2 实际 schema 为准；单位 mm；坐标约定；可选 plan.dxf 确认图）

## 锚点 2：DXF（+building.json）
（v2 管线交付物；逐层 DXF 命名；building.json 与 plan.json 的对应关系）

## 锚点 3：IFC
（aiifc 段：脚本 PARAMS 从 plan/building 派生；designKey/Pset_AIIFC 追溯锚点；版本对应）

## 确认门禁
每个锚点交付前设计师确认；反例 fixture 说明（缺必填字段的 plan.json 应被子 Agent 拒收并报告缺哪些字段）。
```

`references/fixtures/plan.sample.json`：按 v2 真实 schema 给一个最小正例（从 `skills/aidxfv/v2/` 的既有样例/test fixture 复制简化，注明来源）。

- [ ] **Step 7: 写 opencode 示例 agent 配置**

`examples/opencode/agent/aibim-orchestrator.md`（主 agent：description + 编排 prompt 引用本包）、`ifc-agent.md`、`cad-agent.md`（子 agent：低权限提示、指向对应 skill）。格式照 opencode agent markdown 惯例（frontmatter description + 正文 prompt），每个文件头部注释「复制到 .opencode/agent/ 使用」。

- [ ] **Step 8: requirements.txt / CHANGELOG / 跑测试**

```bash
python -m pytest tests/skill/ -q
```
Expected: 全 PASS（含新 orchestrator 两条）。

- [ ] **Step 9: Commit**

```bash
git add skills/aibim-orchestrator tools/skill_pack.py tests/skill/test_skill_pack.py
git commit -m "feat(skills): aibim-orchestrator 主 Agent 编排提示词包——意图路由 + 子 Agent 契约 + plan→cad→ifc 接力数据契约（W-0029 主体）"
```

---

### Task 7: W-0017 关闭 + spec 修订 + 收口同步（W-0029 收尾 + 全局收口）

**Files:**
- Modify: `docs/work/items/W-0017-orchestrator-agent.md`（状态 done + 形态变更说明）
- Modify: `docs/work/items/W-0028-skill-versioned-release.md`、`W-0029`（状态 done）
- Modify: `docs/superpowers/specs/2026-08-11-orchestrator-design.md`（头部修订说明）
- Modify: `docs/superpowers/specs/2026-08-11-platform-framework-design.md`（§5 修订）
- Modify: `docs/work/PLAN-v0.1.0.md`（v0.5 行勾掉）
- Modify: `AGENTS.md`（skill 打包行测试计数同步；「项目是什么」推荐项一句改为提示词包形态）
- Modify: `docs/site/guide/skills.md` + `docs/site/en/guide/skills.md`（补 aibim-orchestrator 行，若 Task 3 留了位）

**Interfaces:**
- Consumes: Task 6 交付的 aibim-orchestrator；Task 2-5 全部完成。

- [ ] **Step 1: W-0017 关闭**

状态改 `done`，关闭于填本迭代分支/PR；背景段末追加：

```markdown
## 形态变更（2026-08-12 用户裁决）

落地形态由「代码级 orchestrator（Eino 评估方向）」改为「agent-agnostic 提示词包 + 数据契约」（skills/aibim-orchestrator，spec 2026-08-12-portability-reuse-design.md §4）。代码级编排与 A2A 出口不再追求；已交付的 notify 事件化/事件 URI 化保留为平台地基，不被提示词包依赖。
```

- [ ] **Step 2: 两份 spec 修订**

- `2026-08-11-orchestrator-design.md` 头部状态行下加修订块：`> 修订（2026-08-12）：本文的代码级 orchestrator/Eino 方向退役为「不推荐」；落地形态改为提示词包（skills/aibim-orchestrator），见 2026-08-12-portability-reuse-design.md §4。事件总线规约（Pure Core/Shell、事件 URI）仍然有效。`
- `2026-08-11-platform-framework-design.md` §5：落地形态改为提示词包——「内容」行改为：主 Agent 编排提示词包（skills/aibim-orchestrator：意图路由 + 子 Agent 分工契约 + plan→cad→ifc 接力数据契约）+ 事件总线（已交付，地基保留）；「落地节奏」行改为：notify 事件化（已交付）→ 提示词包（v0.5 交付）→ 代码级编排不再追求。§1 表格「推荐形式」行同步一句。

- [ ] **Step 3: AGENTS.md 同步**

- 「项目是什么」推荐项一句：orchestrator + 事件总线 → 主 Agent 提示词包（skills/aibim-orchestrator）+ 事件总线（地基）。
- skill 打包行测试计数：跑 `python -m pytest tests/skill/ -q` 拿最新数，替换（147 → 实际值）。

- [ ] **Step 4: skills.md 补行 + item 收口 + PLAN 勾选**

skills.md（zh+en）skill 表格补 aibim-orchestrator 行（如 Task 3 已占位则替换占位）。W-0028、W-0029 状态 done。PLAN v0.5 行标 ✅（PR 号合并后由收尾 commit 或下个迭代补——本任务填「PR 待提」与既有风格一致）。

- [ ] **Step 5: 全量验证**

```bash
python -m pytest tests/skill/ -q
cd docs && npm run docs:build && npm run check:api
```
Expected: 全绿（check:api 应无漂移——本迭代未改 API）。

- [ ] **Step 6: Commit**

```bash
git add docs/work docs/superpowers/specs AGENTS.md docs/site/guide/skills.md docs/site/en/guide/skills.md
git commit -m "docs: W-0017 关闭（形态改提示词包）+ orchestrator/框架 spec 修订 + AGENTS 计数同步 + PLAN v0.5 勾选"
```

---

## Self-Review 记录

- Spec 覆盖：§2→Task 4/5；§3→Task 2/3；§4→Task 6/7；§5→Task 1（W-0030）；§6 工作项→Task 1、PLAN→Task 1/7；§7 纪律→Global Constraints。Dockerfile「补」经探索修正为「既有，冒烟+文档对账」（Task 5）。
- 占位符：Task 4/6 的文档正文给出结构+事实来源指针（以代码/v2 schema 为准），属文档任务合理粒度；代码段均给出真实实现。
- 类型一致：`skill_version(skill_root: Path) -> str` 在 Task 2 定义、Task 2 测试消费；registry key `aibim-orchestrator` 在 Task 6 测试与实现一致。
- 风险：Task 5 需要本机 docker；Task 6 Step 6 依赖读 aidxfv v2 真实 schema（实施时先读后写，不臆造）。
