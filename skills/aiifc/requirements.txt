# aiifc skill 运行时依赖（flows/*.py 与 tools 需要）
# 安装：uv pip install -r requirements.txt （或复用已含这些库的 venv）
ifcopenshell>=0.8
ifcquery>=0.8
numpy>=1.21
ezdxf>=1.3        # 可选：workflow plan→DXF 阶段（dxf_from_design.py）
