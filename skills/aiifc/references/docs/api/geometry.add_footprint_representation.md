# geometry.add_footprint_representation

## API Definition

```python
def add_footprint_representation(model, context: entity_instance, curves: list[entity_instance]) -> entity_instance
```

*Source: api/geometry/add_footprint_representation*

## Import Surface

- run: `ifcopenshell.api.run("geometry.add_footprint_representation", model, ...)`
- direct: `import ifcopenshell.api.geometry; ifcopenshell.api.geometry.add_footprint_representation(model, ...)`

## Description



## Parameters

- **context** (`entity_instance`)
- **curves** (`list[entity_instance]`)
## Returns

entity_instance
