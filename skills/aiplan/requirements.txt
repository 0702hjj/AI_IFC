# aiplan 自包含依赖（独立 venv，不借下游 skill 环境）
# aiplan_tools 可编辑安装：console_scripts（aiplan-*）供 agent 运行期调用，源码只读
--editable ./scripts/packages/aiplan_tools
jsonschema
shapely
# 可选：ezdxf —— 仅 translate_golden 的 golden 坐标诊断（source_dxf_bbox）用，
# 缺失时自动降级跳过（coord_report.note 标注），不列入运行期依赖
